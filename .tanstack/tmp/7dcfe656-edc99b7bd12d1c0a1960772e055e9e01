import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/main/consigli-e-trucchi/')({
  component: RouteComponent,
})

function RouteComponent() {
  return <div>Hello "/main/consigli-e-trucchi/"!</div>
}
