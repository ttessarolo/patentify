import type { JSX } from 'react';
import { ScreenStub } from '../../components/screen-stub';

export default function HomeScreen(): JSX.Element {
  return <ScreenStub title="Home" subtitle="Dashboard native in preparazione" />;
}
