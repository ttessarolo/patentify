import { defineConfig } from '@tanstack/start/config';

export default defineConfig({
  // Configurazione TanStack Start
  // Le route sono gestite automaticamente tramite file-system routing in app/routes/
});
